export type Functions = {
  COUNT: {
    returns: number
    types: ['*']
  }
  AVG: {
    returns: number
    types: [number]
  }
  SUM: {
    returns: number
    types: [number]
  }
  MIN: {
    returns: number
    types: [number]
  }
  MAX: {
    returns: number
    types: [number]
  }
}
